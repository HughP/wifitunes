1) BUILD
To build the preference pane run
./setup.py py2app
from this directory.

2) DMG
To make a disk image run
./makeimeage.sh
from this directory.
Have a look at the script before running, if you want to know what it is (not) doing.

3) License
Look into about.rtfd for licensing information
